<UserControl x:Class="FileManagerUI.Custom_Controls.BackButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileManagerUI.Custom_Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Style="{DynamicResource ButtonStyle1}" x:Name="BackBtn">
            <Button.Content>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Image Margin="5,0,0,0" Width="50" Stretch="Uniform" Source="/assets/left.png" HorizontalAlignment="Left"/>
                        <TextBlock Margin="5,0,0,0" Text="Back" VerticalAlignment="Center" TextAlignment="Left" HorizontalAlignment="Left" FontWeight="Heavy" Foreground="LightSlateGray"/>
                    </StackPanel>
                </Grid>
            </Button.Content>
        </Button> 
    </Grid>
</UserControl>
