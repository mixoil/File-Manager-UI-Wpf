<UserControl x:Class="FileManagerUI.Custom_Controls.Folder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileManagerUI.Custom_Controls"
             mc:Ignorable="d">
    <Grid>
        <Button Style="{DynamicResource ButtonStyle1}" x:Name="FolderButton">
            <Button.Content>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Image Margin="5,0,0,0" Width="50" Stretch="Uniform" Source="/assets/folder.png" HorizontalAlignment="Left"/>
                        <TextBlock Margin="5,0,0,0" Text="{Binding Path=FolderName, Mode=TwoWay}" VerticalAlignment="Center" TextAlignment="Left" HorizontalAlignment="Left" FontWeight="Bold" Foreground="LightSlateGray"/>
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="{Binding Path=ItemsCount, Mode=TwoWay}" VerticalAlignment="Center" TextAlignment="Left" HorizontalAlignment="Left" Foreground="LightSlateGray"/>
                    <TextBlock Grid.Column="2" Text="{Binding Path=LastModified, Mode=TwoWay}" VerticalAlignment="Center" TextAlignment="Left" HorizontalAlignment="Left" Foreground="LightSlateGray"/>
                    <TextBlock Grid.Column="3" Text="{Binding Path=FolderSize, Mode=TwoWay}" VerticalAlignment="Center" TextAlignment="Left" HorizontalAlignment="Left" Foreground="LightSlateGray"/>
                </Grid>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
